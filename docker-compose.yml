version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-ai-assistant
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Taipei
    command: python bot.py
